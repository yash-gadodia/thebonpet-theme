{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "bash -c 'path=\"$CLAUDE_TOOL_INPUT_file_path\"; case \"$path\" in *.env|*.env.*|*.pem|*.key|*.cert|*credential*|*secret*) echo \"Refusing to write to credential-sensitive path: $path\" >&2; exit 2 ;; *) exit 0 ;; esac'"
          }
        ]
      }
    ]
  }
}
